# Components/Blocks

These are general componets reused between sites whose content can be set in the CMS. Configuration is located in `blocks.config.ts`

# Components/Custom

These are 'custom' or one-off components that are specific to a particular site.

# Components/

These are low level components that are used to render other components (i.e. buttons, startup cards etc)